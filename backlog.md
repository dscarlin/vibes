# Backlog

## Platform
- Customer notifications (per-project webhook/email on deploy fail, offline, quota hit).
- Health SLOs (success rate + median deploy time per environment).
- Per-project healthcheck overrides (per env path/protocol).
- Cancel task feature (user-initiated cancellation for queued/running tasks).

## Go-Live Readiness
- Enable automated RDS backups + deletion protection + copy tags to snapshots (closer to go-live).
