You are modifying this disposable clone of the Vibes Platform repository.

This clone has already been rewired so the task-scoped environment is the default and only environment you should use for this task.

Hard rules:
- Do not edit, commit, or depend on `.vp-task/`, `deploy/.generated/`, root `.env*`, or `.codex-last-message.txt` for feature changes. They are generated task plumbing and will be removed before publish.
- Do not run `git commit`, `git push`, `git rebase`, `git reset`, or change git remotes. The wrapper handles publish.
- Stay inside this clone. Do not switch to shared/live environments.
- Use the generated task wrappers when you need to redeploy, validate, or inspect logs.

Repo/task context:
- Feature branch: {{FEATURE_BRANCH}}
- Base branch: {{BASE_BRANCH}}
- Task manifest: {{MANIFEST_PATH}}
- Task commands: {{COMMANDS_PATH}}
- Platform API: {{API_URL}}
- Platform app: {{APP_URL}}
- Task root host: {{ROOT_HOST}}

Generated commands available in this clone:
- `./.vp-task/bin/platform-urls`
- `./.vp-task/bin/validate-feature`
- `./.vp-task/bin/status`
- `./.vp-task/bin/redeploy-platform`, `validate-platform`, `tail-server`, `tail-worker`, and `tail-web` exist for manual replica debugging only. Do not use them unless the user explicitly asks for replica deployment/debugging.

Execution expectations:
- Read the affected code paths before editing.
- Implement the requested change directly in this repo.
- Run targeted validation that matches the change you made.
- If `./.vp-task/bin/validate-feature` is configured for this run, use it when your change needs feature-specific acceptance checks.
- Do not build/push/apply the task replica or run full `validate-platform` unless the user explicitly asks for that expensive workflow.
- The outer runner will not perform full replica validation after you finish. Your responsibility is to run the targeted validations that actually match the change.
- Keep going until the feature is implemented and validated or you hit a concrete blocker you can explain.

Task request:
{{PROMPT}}
