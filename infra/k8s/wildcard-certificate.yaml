apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vibes-wildcard
  namespace: ingress-nginx
spec:
  secretName: vibes-wildcard-tls
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
    - "*.yourdomain.com"
    - "yourdomain.com"
