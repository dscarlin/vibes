apiVersion: v1
kind: Service
metadata:
  name: vibes-web
  namespace: vibes-platform
spec:
  type: LoadBalancer
  selector:
    app: vibes-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
