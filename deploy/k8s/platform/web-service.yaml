apiVersion: v1
kind: Service
metadata:
  name: ${PLATFORM_WEB_NAME}
  namespace: ${PLATFORM_NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: ${PLATFORM_WEB_NAME}
  ports:
    - name: http
      port: 80
      targetPort: 8080
